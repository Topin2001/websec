# websec
## Project description: 
La creation d'un docker pour présenter la faille à  la classe

## Start the docker:
  ### Create the image :
    docker build . -t bank
  ### Students: 
    docker run -p 3000:3000 bank
